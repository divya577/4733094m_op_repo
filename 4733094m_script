#!/bin/bash 

# Update system
apt-get update 

# Ensure git is installed using Puppet
puppet resource package git ensure=present 

# Ensure Apache2 is installed using Puppet
puppet resource package apache2 ensure=present 

# Ensure Apache2 service is running using Puppet
puppet resource service apache2 ensure=running 

# Ensure /tmp/clone is absent and then a directory (like Puppet's file management)
puppet resource file /tmp/4733094m/research ensure=absent force=true
puppet resource file /tmp/4733094m/research ensure=directory 

# Clone the repository
git clone https://github.com/divya577/4733094m_op_repo.git /tmp/4733094m/research

# Replace index.html with the one from the cloned repo (similar to Puppet file resource)
cp -p /tmp/4733094m/research/4733094m_op_repo/index.html /var/www/html
